#include <stdio.h>
#include <stdlib.h>

int main(){
    /***
     * Tipo de dado: define o conjunto de valores
     * (domínio) que uma variável pode assumir.
     * 
     * Ex: int
     * 
     * ... -2, -1, 0, +1, +2,...
     * 
     * Estruturas de Dados: trata-se de um 
     * relacionamento lógico entre tipos de dados 
     * 
     * Tipos Abstratatos de Dados ou "TAD": incluem
     * as operações para a manipulação dos dados
     * 
     * Ex:
     *    - criação da estrutura
     *    - inclusão de um elemento
     *    - remoção de um elemento
     *    - acesso a um elemento
     *    - etc
     * 
     *  Vantagens: 
     *  - Encapsulamento e Segurança: usuário não tem
     * acesso direto aos dados.
     * 
     *  - Flexibilidade e Reutulização: podemos alterer
     * o "TAD" sem alterar as aplicações que o utilizam.
     * 
     * Exemplo de TAD: arquivos em C
     * 
     * File* f;
     * 
     * Os dados de "f" somente podem ser acessados
     * pelas funções de manipulação do tipo: 
     *  - fopen()
     *  - fclose()
     *  - fputc()
     *  - fgetc()
     *  - feof()
     *  - ...  
     * **/
    
    return 0;
}